defaultTasks "clean", "buildRpm"

allprojects {
    group = "com.rti"
}

project(":rti-connext-dds-$versionMajor$versionMinor-admin-console") {
    version = "$versionMajor.$versionMinor.0.0"
    rpmRelease = "1"
}

project("rti-connext-dds-$versionMajor$versionMinor-license") {
    version = "1.0"
    rpmRelease = "$rpmRelease"
}

project("rti-connext-dds-$versionMajor$versionMinor-persistence-service") {
	version = "$versionMajor.$versionMinor.0.0"
	rpmRelease = "$rpmRelease"
}

project("rti-connext-dds-$versionMajor$versionMinor-routing-service") {
	version = "$versionMajor.$versionMinor.0.0"
	rpmRelease = "$rpmRelease"
}

project("rti-connext-dds-$versionMajor$versionMinor-runtime-pro-x64Linux2.6gcc4.4.5") {
	version = "$versionMajor.$versionMinor.0.0"
	rpmRelease = "$rpmRelease"
}

project("rti-connext-dds-$versionMajor$versionMinor-runtime-pro-x64Linux3gcc4.8.2") {
	version = "$versionMajor.$versionMinor.0.0"
	rpmRelease = "$rpmRelease"
}

project("rti-connext-dds-$versionMajor$versionMinor-sdk-pro") {
	version = "$versionMajor.$versionMinor.0.1"
	rpmRelease = "$rpmRelease"
}

project("rti-connext-dds-$versionMajor$versionMinor-sdk-pro-sources") {
	version = "$versionMajor.$versionMinor.0.0"
	rpmRelease = "$rpmRelease"
}

project("rti-connext-dds-$versionMajor$versionMinor-sdk-pro-target-x64Linux2.6gcc4.4.5") {
	version = "$versionMajor.$versionMinor.0.0"
	rpmRelease = "$rpmRelease"
}

project("rti-connext-dds-$versionMajor$versionMinor-sdk-pro-target-x64Linux3gcc4.8.2") {
	version = "$versionMajor.$versionMinor.0.0"
	rpmRelease = "$rpmRelease"
}
